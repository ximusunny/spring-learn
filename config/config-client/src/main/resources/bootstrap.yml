spring:
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:7001
      name: didispace
      discovery:
        enabled: true
        service-id: config-server

  application:
    name: config-client-1
